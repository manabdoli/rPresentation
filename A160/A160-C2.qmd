---
title: "A. Set Theory: Set Operations"
author: "Mansour Abdoli, PhD"
format:
  live-revealjs:
    slide-number: true
    incremental: true
    chalkboard:
        buttons: true  # Shows icons for easy access
    preview-links: true
    code-overflow: wrap
    footer: "A.Set Theory-D"
#resources:
#  - assets/webr_helpers.R
engine: knitr
execute:
  echo: true
  warning: false
  message: false
#output-files: CK-chx.x-y.y.html
params:
  load_helper: false
  showDetails: false
---

## Concept Overview

* Union ($A \cup B$)
* Intersection ($A\cap B$)
* Difference ($A-B$)
* Universal set (U or S)
* Complement ($A^c = U-A$)

{{< include ../A100/_extensions/r-wasm/live/_knitr.qmd >}}

```{webr}
#| echo: false
#| output: false
#| warning: false
#| message: false

#  Packages available in your WebR build -

# Load your helper functions (optional)
if(isTRUE(params$load_helper)){
  helpers <- readLines("assets/webr_helpers.R", warn = FALSE)
  eval(parse(text = helpers))
}

options(digits = 3)

```

```{r}
#| echo: false
# Helper function
# suppressPackageStartupMessages({
# })

# if(isTRUE(params$load_helper)){
#   source('assets/webr_helpers.R')
# }
# add any helper functions needed

```


```{r, echo=FALSE, message=FALSE, warning=FALSE}
knitr::opts_chunk$set(echo = FALSE)
# suppressPackageStartupMessages({
# })

showDetails <- isTRUE(params$showDetails)
hideText <- function(text, show=showDetails){
  if(show) text else
    funs2teach::hideText(text, docType = 'html')
}

options(knitr.kable.NA = '')
set.seed(123)
```
# Measures of Position

### Population and Sample Z-Score ($z$):

### $k^{th}$ Percentile ($P_{k}$ or $X_{\alpha}$, $\alpha = 1 - k\text{/}100$):

### Median or Second Quartile ($Q_{2}$):

### First Quartile ($Q_{1}$):

### 

### Third Quartile ($Q_{3}$):

### Five-Number Summary: Min, Q1, Med, Q2, Max

### Box and Whisker Plot (Boxplot):

# Outliers & IQR 

### Outlier:

### Inter-Quartile Range ($IQR$):

### $1.5\ IQR$ Rule: Lower and Upper Fences

### Modified Boxplot:

# Questions

### When data has a bell-shape distribution, what is the proportion of observations below $\mu - 2\sigma$? (Use empirical rule.)

### What is the $16^{th}$ percentile of a data has a bell-shape distribution with mean $\mu$ and standard deviation $\sigma$?

### ![Horizontal, side-by-side boxplots, x with an outlier tends to be lower than y.](media/image01.png){width="4.587496719160105in" height="2.415999562554681in"}

### Consider the above boxplots for variables $x$ and $y$. 

### Which one has an outlier?

### What is the median of $y$?

### Comparing $x$ and $y$, which one is greater? What measure did you used?

### Comparing $x$ and $y$, which one has higher dispersion?

### $75\%$ of observations in which variable is greater than $22$?

### $75^{th}$ percentile of which variable is $22$?
